<% @user ||= current_user %>
<%# needed b/c @user is nil on the home page%>

<div class="stats">
	<a href="<%= following_user_path(@user) %>">
		<strong id="following" class="stat">
			<% @user.followed_users.count %>
		</strong>
		following
	</a>
	<a href="<%= folled_user_path(@user) %>">
		<strong id="following" class="stat">
			<% @user.followers.count %>
		</strong>
		followers
	</a>
</div>