<% if signed_in? %>
	<%= render 'shared/signed_in_home' %>
<% else %>
	<%= render 'shared/new_user_home' %>
<%# no need to say render 'new_user_home.html.erb' - that's deprecated %>

<%# moved signed_in_home and new_user_home into shared folder b/c 
they sort of indirectly get called from microposts_controller and 
they weren't being found when they were just in the static_pages folder %>

<% end %>

